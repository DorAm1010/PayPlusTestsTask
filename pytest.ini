[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    api: API-only tests against the PayPlus REST API (no browser)
    ui: Selenium UI tests against the hosted payment page
    e2e: Combined API + Selenium end-to-end tests
